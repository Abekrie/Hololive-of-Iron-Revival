baelz_council_economic_sphere_category = {

	baelz_invests_in_your_industry_dec = {

		icon = generic_construction

		days_re_enable = 270

		allowed = {
			
		}

		state_target = yes
		target_trigger = {
			FROM = {
				AND = {
						OWNER = {
							is_in_faction_with = ROOT
							NOT = { tag = ROOT }
						}
						is_capital = yes
					}
			}
		}

		on_map_mode = decision_view_only

		cost = 50

		visible = {
		    tag = BAL
			OR = {has_country_flag = Can_do_council_economic_sphere
			is_debug = yes}
		}

		available = {
			NOT = { has_country_flag = on_going_investment_bal }
		}

		days_remove = 30

		complete_effect = {
			set_country_flag = on_going_investment_bal
		}

		remove_effect = {
			clr_country_flag = on_going_investment_bal
			FROM.OWNER = {
				country_event = { id = baelz.3 hours = 12 }
			}
		}

		ai_will_do = {
			factor = 200
			modifier = {
				factor = 200
				is_historical_focus_on = yes
			}
		}
	}

	baelz_invests_in_our_own_industry_dec = {

		icon = generic_construction

		days_re_enable = 270

		allowed = {
			
		}

		cost = 100

		visible = {
		    tag = BAL
			OR = {has_country_flag = Can_do_council_economic_sphere
			is_debug = yes}
		}

		available = {
			NOT = { has_country_flag = on_going_investment_bal }
		}

		days_remove = 30

		complete_effect = {
			set_country_flag = on_going_investment_bal
		}

		remove_effect = {
			clr_country_flag = on_going_investment_bal
			add_offsite_building = { type = industrial_complex level = 1 }
		}

		ai_will_do = {
			factor = 200
			modifier = {
				factor = 200
				is_historical_focus_on = yes
			}
		}
	}
}