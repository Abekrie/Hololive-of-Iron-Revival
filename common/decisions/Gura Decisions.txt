gura_shark_activity_category = {

	gura_shark_infested_area_dec = {

		icon = generic_gura

		#days_re_enable = 10

		allowed = {
			
		}

		state_target = yes
		target_trigger = {
			FROM = {
				OWNER = {
					num_of_controlled_states > 2

					NOT = {tag = ROOT
					tag = SOV
					tag = ITA
					#tag = JAP
					tag = USA
					tag = ENG
					tag = GER
					tag = FRA
					}

					NOT = {
						num_of_factories > 50
					}
					NOT = {
						has_war_with = ROOT
						is_in_faction_with = ROOT
						is_subject_of = ROOT
					}
					}
				ROOT = {
					NOT = { is_subject_of = FROM.OWNER }
				}
				NOT = {
					has_state_category = megalopolis
					has_state_category = metropolis
					has_state_category = large_city
					has_state_category = large_town
					has_state_category = city
					has_state_category = town
				}
				
				NOT = {
					region = 26
					region = 130
					region = 135
					region = 134
					region = 37
					region = 9

					state = 685
					state = 453
					state = 410
					state = 413
					state = 678
					state = 559
					state = 546
					state = 547
					state = 551
					state = 295
					state = 423
					state = 685
					state = 669
					state = 683
					state = 682
					state = 471
					state = 495
					state = 666
					state = 148
				}
				is_coastal = yes
				is_capital = no
			}
		}

		on_map_mode = map_only

		cost = 150

		visible = {
			tag = GAW
			has_country_flag = Gura_can_shark_infested_area
		}

		available = {
			NOT = {
				has_country_flag = gura_ongoing_shark_infestation
			}
		}

		days_remove = 250

		complete_effect = {
			set_country_flag = gura_ongoing_shark_infestation
		}

		remove_effect = {
			clr_country_flag = gura_ongoing_shark_infestation
			FROM = { set_state_flag = shark_infested_state_flag }
			FROM.OWNER = {
				country_event = {
					id = gura.8
				}
			}
		}

		ai_will_do = {
			factor = 200
			modifier = {
				factor = 200
				is_historical_focus_on = yes
			}
		}
	}

	gura_build_royal_dockyard_dec = {

		icon = generic_gura

		#days_re_enable = 10

		allowed = {
			
		}

		state_target = yes
		target_trigger = {
			FROM = {
				AND = {
						CONTROLLER = {
							tag = ROOT
						}
						is_core_of = ROOT
						NOT = {
								has_state_flag = GAW_already_build_dock
						}
					}
			}
		}

		on_map_mode = map_only

		cost = 50

		visible = {
		    FROM.OWNER = {
				OR = {
					tag = GAW
				}
				has_country_flag = Gura_can_build_royal_dockyard
			}
		}

		available = {
			
		}

		days_remove = 60

		remove_effect = {
			FROM = {
			add_extra_state_shared_building_slots = 1
		    add_building_construction = {
					type = dockyard
					level = 1
					instant_build = yes
				}
			}
			FROM = {set_state_flag = GAW_already_build_dock}
		}

		ai_will_do = {
			factor = 200
			modifier = {
				factor = 200
				is_historical_focus_on = yes
			}
		}
	}



}