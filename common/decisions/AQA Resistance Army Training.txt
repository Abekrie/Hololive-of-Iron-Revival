aqua_hololive_resistance_category = {

	aqua_inspect_the_resistance_army_dec = {

		icon = generic_army_support

		days_re_enable = 280

		allowed = {
			
		}

		available = {
			NOT = {
				has_country_flag = on_going_resistance_training
			}
		}

		cost = 25

		visible = {
			tag = AQA
		}

		remove_effect = {
			clr_country_flag = on_going_resistance_training
		}

		modifier = {
			experience_gain_army = 0.8
		}

		days_remove = 30

		complete_effect = {
			set_country_flag = on_going_resistance_training
		}

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 100
				is_historical_focus_on = yes
			}
		}
	}

	aqua_military_training_with_the_resistance_dec = {

		icon = generic_army_support

		days_re_enable = 365

		allowed = {
			
		}

		available = {
			NOT = {
				has_country_flag = on_going_resistance_training
			}
		}

		state_target = yes
		target_trigger = {
			FROM = {
				AND = {
					OWNER = {
						OR = {
							is_in_faction_with = AQA
							is_subject_of = AQA
						}
						NOT = { tag = AQA }
					}
					is_capital = yes
				}
			}
		}

		on_map_mode = decision_view_only

		cost = 25

		visible = {
			tag = AQA
			OR = {
				has_country_flag = Aqua_can_train_resistance_army
			}
		}

		remove_effect = {
			clr_country_flag = on_going_resistance_training
			FROM.OWNER = {
				army_experience = 35
			}
		}

		modifier = {
			experience_gain_army = 0.25
		}

		days_remove = 30

		complete_effect = {
			set_country_flag = on_going_resistance_training
			FROM.OWNER = {
				country_event = { id = aqua.71 hours = 12 }
			}
		}

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 100
				is_historical_focus_on = yes
			}
		}
	}
}