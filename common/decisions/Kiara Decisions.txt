kiara_kfp_category = {

	kiara_open_new_kfp_dec = {

		icon = generic_kiara

		#days_re_enable = 10

		allowed = {
			
		}

		state_target = yes
		target_trigger = {
			FROM = {
				AND = {
						CONTROLLER = {
							OR = { tag = KIA }
						}
						is_core_of = KIA
						NOT = {
								has_state_flag = KIA_already_developed
						}
					}
			}
		}

		on_map_mode = map_only

		cost = 75

		visible = {
			tag = KIA
			has_country_flag = Kiara_can_build_kfp
		}

		available = {
			
		}

		days_remove = 60

		remove_effect = {
			FROM = {
			add_extra_state_shared_building_slots = 1
		    add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			FROM = {set_state_flag = KIA_already_developed}
		}

		ai_will_do = {
			factor = 200
			modifier = {
				factor = 200
				is_historical_focus_on = yes
			}
		}
	}


}