watame_sheep_house_category = {

	watame_sheep_house_dec = {

		icon = generic_construction

		#days_re_enable = 10

		allowed = {
			
		}

		state_target = yes
		target_trigger = {
			FROM = {
				AND = {
						CONTROLLER = {
							OR = {
								tag = ROOT
								AND = {
									tag = HTO
									is_in_faction_with = ROOT
								}
							}
						}
						OR = {
							is_core_of = ROOT
							is_core_of = HTO
							}
						NOT = {
								has_state_flag = WAT_already_developed
						}
					}
			}
		}

		on_map_mode = map_only

		cost = 75

		visible = {
		    FROM.OWNER = {
				OR = {
					tag = WAT
				}
				has_country_flag = WAT_can_sheep_house
			}
		}

		available = {
			
		}

		days_remove = 60

		remove_effect = {
			FROM = {
			add_extra_state_shared_building_slots = 1
		    add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			FROM = {set_state_flag = WAT_already_developed}
		}

		ai_will_do = {
			factor = 200
			modifier = {
				factor = 200
				is_historical_focus_on = yes
			}
		}
	}
}