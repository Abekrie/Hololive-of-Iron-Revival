noel_shirogane_federation_category = {
	noel_establish_a_puppet_state_dec = {

		icon = generic_form_nation

		#days_re_enable = 180

		allowed = {
			
		}

		state_target = yes
		target_trigger = {
			FROM = {
				AND = {
						OWNER = {
							OR = {
								tag = RUS
								tag = SUB
							}
						}
						is_capital = yes
					}
			}
		}

		on_map_mode = map_and_decisions_view

		cost = 200

		visible = {
		    tag = NOE
			has_country_flag = noel_can_shirogane_federation
			FROM.OWNER = { 
				NOT = {has_country_flag = noel_establish_a_puppet_state_dec_FLAG} 
			}
		}

		available = {
			is_subject = no
			FROM.OWNER = {
				is_in_faction_with = NOE
				is_subject = no
			}
		}

		#days_remove = 365

		complete_effect = {
			FROM.OWNER = {
				country_event = { id = noel.46 }
				set_country_flag = noel_establish_a_puppet_state_dec_FLAG
			}
		}

		#remove_effect = {}

		ai_will_do = {
			factor = 10
			modifier = {
				factor = 10
				is_historical_focus_on = yes
			}
		}
	}

	noel_knight_exercise_dec = {

		icon = generic_army_support

		#days_re_enable = 10

		allowed = {
			
		}

		cost = 50

		visible = {
		    tag = NOE
			has_country_flag = noel_can_shirogane_federation
		}

		available = {
			
		}

		days_remove = 40

		complete_effect = {
			army_experience = 25
			every_country = {
				limit = { is_in_faction_with = NOE }
				army_experience = 5
			}
		}

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 100
				is_historical_focus_on = yes
			}
		}
	}
}